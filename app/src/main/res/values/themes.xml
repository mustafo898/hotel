<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Hotel" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/white</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>

    <style name="Theme.Hotel" parent="Base.Theme.Hotel" />

    <style name="edit_text">
        <item name="boxStrokeColor">@color/white_transparent</item>
        <item name="boxBackgroundColor">@color/white_transparent</item>
        <item name="boxCornerRadiusTopStart">8dp</item>
        <item name="boxCornerRadiusBottomEnd">8dp</item>
        <item name="boxCornerRadiusBottomStart">8dp</item>
        <item name="boxCornerRadiusTopEnd">8dp</item>
        <item name="boxStrokeWidth">0dp</item>
        <item name="hintTextColor">@color/grey</item>
    </style>
</resources>